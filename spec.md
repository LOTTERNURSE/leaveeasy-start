# spec.md — สรุประบบ LeaveEasy (Module 2)

> ฉบับนี้แก้จากรอบก่อน: รอบก่อนสรุปจากชื่อ commit ใน git log เป็นหลัก ("มีคอมมิตเพิ่ม CRUD" → เข้าใจว่าเขียนจริงหมดแล้ว) ซึ่ง**ไม่ตรงกับที่โค้ดทำจริง** ฉบับนี้เปิดอ่านทุกไฟล์ `js/*.js` ที่แตะข้อมูลจริง แล้วไล่ทีละบรรทัดว่า "เขียนลง Firestore จริง" หรือ "แค่แก้ตัวแปรในหน่วยความจำ/sessionStorage แล้วดูเหมือนเสร็จ" — ยึดพฤติกรรมโค้ดเป็นหลัก ไม่ใช่คอมเมนต์ในโค้ดหรือชื่อ commit

---

## 1. หน้าจอทั้งหมด

มีไฟล์ `.html` จริง 9 ไฟล์:

| ไฟล์ | หน้าที่ |
|---|---|
| `index.html` | หน้าแรก รวมลิงก์ไปหน้าอื่น |
| `leave-requests.html` | รายการใบลา |
| `new-leave-request.html` | ฟอร์มยื่นใบลาใหม่ + ปุ่ม AI จัดประเภท |
| `leave-request-detail.html` | รายละเอียดใบลา + อนุมัติ/ไม่อนุมัติ + ความเห็น + ลบ + AI สรุป |
| `leave-types.html` | จัดการประเภทการลา |
| `login.html` / `register.html` | เข้าสู่ระบบ / สมัครสมาชิก |
| `ai-test.html` | หน้าทดสอบเรียก AI ของผู้พัฒนา ไม่ใช่หน้าที่ผู้ใช้ปลายทางเห็น |
| `404.html` | หน้าไม่พบ |

**ไม่มีไฟล์จริง:** `dashboard.html` — สเปกออกแบบไว้เป็นหน้าที่ 5 แต่ตั้งใจเลื่อนไป Module 3

ทุกหน้า (ยกเว้น login/register) มี `js/auth-guard.js` เช็คว่าล็อกอินหรือยัง — ยังไม่ล็อกอินจะเด้งไป `login.html`

---

## 2. โครงสร้างข้อมูล — และสถานะจริงของแต่ละส่วน

โครงร่างที่ตั้งใจไว้ (`leaveeasy-spec.md`):

```
users/{id}              { name, email, role }
leaveTypes/{id}          { name }
leaveRequests/{id}       { title, reason, status, requesterId, requesterName,
                           approverId, approverName, leaveTypeId, leaveTypeName,
                           startDate, endDate, createdAt, aiSuggestion }
  leaveRequests/{id}/approvals/{id}   { authorId, authorName, message, createdAt }
  leaveRequests/{id}/aiLog/{id}       { input, output, createdAt }
```

**แต่ตอนนี้แต่ละ action เขียนลง Firestore จริงไม่เท่ากัน** — ตรวจจากโค้ดจริงทีละไฟล์:

| Action | ไฟล์ | เขียน Firestore จริงไหม |
|---|---|---|
| ดูรายการใบลา | `leave-requests.js` | ✅ อ่านจริงจาก `leaveRequests` — แต่เอาไปต่อท้ายด้วยรายการใบลาที่ค้างใน `sessionStorage` (ดูแถวถัดไป) |
| ยื่นใบลาใหม่ | `new-leave-request.js` | ❌ **ไม่เขียน Firestore เลย** — เก็บลง `sessionStorage["ใบลาที่ยื่นใหม่"]` เท่านั้น ปิดแท็บ/เปิดแท็บใหม่คือหายหรือมองไม่เห็นกัน (sessionStorage ผูกกับแท็บเดียว) |
| ดูรายละเอียดใบลา + ความเห็น | `leave-request-detail.js` | ✅ อ่านจริงจาก `leaveRequests/{id}` และ subcollection `approvals` |
| **อนุมัติ / ไม่อนุมัติ** | `leave-request-detail.js` → `เปลี่ยนสถานะ()` | ❌ **แก้แค่ตัวแปรในหน่วยความจำ** (`ใบ.status = ...`) ไม่มีการเรียก `.update()` ใด ๆ เลย — กด F5 แล้วสถานะกลับเป็น `รอพิจารณา` เหมือนเดิมทุกครั้ง |
| **เขียนความเห็น** | `leave-request-detail.js` → `ส่งความเห็น()` | ❌ **แก้แค่ในหน่วยความจำ** ไม่เขียนลง subcollection `approvals` จริง และยัง**ฮาร์ดโค้ดชื่อผู้เขียนเป็น `u002` / "สมหญิง รักงาน" เสมอ** ไม่ได้อ้างอิงคนที่ล็อกอินอยู่จริง (`firebase.auth().currentUser`) ทั้งที่ระบบมีล็อกอินแล้ว |
| ลบใบลา | `leave-request-detail.js` → `ลบใบลา()` | ✅ เรียก `db.collection("leaveRequests").doc(id).delete()` จริง — ใช้ได้กับใบลาของ**ใครก็ได้**ที่ล็อกอินอยู่ ไม่มีการเช็คว่าเป็นเจ้าของใบหรือไม่ (ฝั่ง UI แค่ปิดปุ่มถ้าสถานะไม่ใช่ `รอพิจารณา` — ไม่ใช่การบังคับจาก Security Rules) |
| AI ช่วยจัดประเภท (ปุ่มในหน้ายื่นใบลาใหม่) | `ai-classify.js` | ไม่เกี่ยวกับ Firestore โดยตรง — แค่เติมค่าในดรอปดาวน์บนหน้าจอ (ผลลัพธ์สุดท้ายก็ไปจบที่ sessionStorage เหมือนแถวที่ 2) |
| AI ช่วยสรุปใบลา (ปุ่มในหน้ารายละเอียด) | `ai-summarize.js` + `leave-request-detail.js` → `สรุปด้วยAI()` | ✅ เขียนจริง — `update({ aiSuggestion })` บนตัวใบลา และเพิ่ม document ใหม่ใน subcollection `aiLog` ทุกครั้งที่เรียก (สำเร็จหรือ error ก็บันทึก) |
| จัดการประเภทการลา (เพิ่ม/แก้/ลบ) | `leave-types.js` | ❌ **ไม่แตะ Firestore เลย** ทำงานกับสำเนาของ `window.LEAVE_DATA.leaveTypes` (ข้อมูลปลอมที่พิมพ์ค้างไว้ใน `js/data.js`) ในหน่วยความจำล้วน ๆ — รีเฟรชหน้าคือค่ากลับไปเป็นชุดเดิม 3 รายการเสมอ |
| ดรอปดาวน์ประเภทการลาในหน้ายื่นใบลาใหม่ | `new-leave-request.js` | ❌ อ่านจาก `window.LEAVE_DATA.leaveTypes` เช่นกัน **ไม่ได้อ่านจากโฟลเดอร์ Firestore `leaveTypes`** ตามที่สเปกตั้งใจไว้ (US-02) — ดังนั้นประเภทที่เพิ่มในหน้าจัดการประเภทก็จะไม่โผล่ที่นี่อยู่ดี เพราะทั้งสองหน้าคนละแหล่งข้อมูลและต่างก็ไม่เขียนจริง |
| สมัครสมาชิก | `register.js` | ✅ สร้าง `users/{uid}` จริง พร้อม `role: "employee"` เสมอ |

**สรุปสั้น:** สิ่งที่ต่อ Firestore จริงตอนนี้มีแค่ — อ่านรายการใบลา, อ่านรายละเอียด+ความเห็น, ลบใบลา, สมัครสมาชิก/ล็อกอิน, และปุ่ม AI สรุป (เขียน `aiSuggestion` + `aiLog`) เท่านั้น ส่วนที่เหลือ (สร้างใบลาใหม่, อนุมัติ/ไม่อนุมัติ, เขียนความเห็น, จัดการประเภทการลา) **ยังเป็นของปลอมในหน่วยความจำ/sessionStorage ทั้งหมด** แม้จะมีปุ่มและหน้าตาครบแล้วก็ตาม

**field ที่จดชื่อซ้ำ (denormalize):** `requesterName`, `approverName`, `leaveTypeName`, `authorName` — คัดลอกจาก `users.name` / `leaveTypes.name` (ตามที่ตั้งใจไว้ในสเปก แต่ก็มีผลจริงเฉพาะ field ที่มาจาก action ที่เขียนจริงเท่านั้น)

**`status` มี 3 ค่า** `รอพิจารณา` / `อนุมัติ` / `ไม่อนุมัติ` — กฎการเปลี่ยนสถานะ (เปลี่ยนได้ทางเดียว, ลบได้เฉพาะ `รอพิจารณา`) มีอยู่จริงแค่ **ฝั่งหน้าจอ** (ซ่อน/ปิดปุ่ม) ยังไม่มีการบังคับจริงฝั่งข้อมูล เพราะการอนุมัติเองก็ไม่ถูกเขียนกลับไปที่ Firestore อยู่แล้ว

**Seed ข้อมูลตัวอย่าง:** `js/seed-data.js` (ปุ่มในเบราว์เซอร์) และ `scripts/seed-firestore.js` (`npm run seed`) — ยังไม่ได้ตรวจโค้ดสองไฟล์นี้ละเอียดในรอบนี้ ยึดคำอธิบายจาก `leaveeasy-spec.md`/`CLAUDE.md` ไปก่อน (ใช้ document ID ตายตัว รันซ้ำได้)

---

## 3. บทบาทผู้ใช้ (role)

`users.role` มี 3 ค่า: `employee` (ผู้ขอลา) · `manager` (ผู้อนุมัติ) · `hr` (ฝ่ายบุคคล) — ความหมายและสิทธิ์ที่ตั้งใจไว้ดูใน [ACL.md](ACL.md)

**สถานะจริงในโค้ดตอนนี้ (สำคัญกว่ารอบก่อนที่บอกไว้):**
- `firestore.rules` เช็คแค่ `request.auth != null` — ล็อกอินแล้วอ่าน/เขียนได้ทุกโฟลเดอร์ ไม่มีการเช็ค `role` เลย
- ในโค้ดฝั่งหน้าเว็บเองก็**ไม่มีจุดไหนอ่านค่า `role` ของผู้ใช้ที่ล็อกอินอยู่เลย** (`leave-request-detail.js` ใช้แค่ `firebase.auth().currentUser` เพื่อรู้ว่าใครล็อกอิน ไม่เคยไปเปิด `users/{uid}` มาดู `role`) ปุ่มอนุมัติ/ไม่อนุมัติ/ลบ จึงแสดงให้ **ผู้ล็อกอินทุกคนเห็นเหมือนกันหมด** ไม่ว่าจะเป็น employee, manager หรือ hr
- สรุป: ตารางสิทธิ์ใน ACL.md ทั้งหมดยังเป็นแค่ "ข้อกำหนดที่ตั้งใจไว้" ไม่มีผลจริงในระบบสักจุดเดียวตอนนี้ — ตรงกับที่ ACL.md เขียนกำกับไว้เองว่าเป็นงานสัปดาห์ที่ 8 ที่ยังไม่ deploy

---

## 4. สิ่งที่ไม่ทำใน Module นี้ (Module 2)

**ด้านเทคนิค**
- ไม่ใช้ framework — HTML/CSS/JS ธรรมดาเท่านั้น
- ไม่เขียนเซิร์ฟเวอร์ของตัวเอง — หน้าเว็บคุยกับ Firestore ตรง ๆ (ข้อยกเว้นเดียวคือเรียก OpenRouter ตรงจากเบราว์เซอร์ ซึ่งสเปกก็บันทึกไว้ว่าเป็นความเสี่ยงที่ตั้งใจยอมรับของ Module 2)
- ไม่ใช้ฐานข้อมูลตาราง (SQL) ไม่มี migration
- ไม่แบ่งหน้า (pagination)

**ด้านฟีเจอร์**
- ไม่มีแดชบอร์ดจริง (`dashboard.html` ไม่มีไฟล์)
- ไม่มีแจ้งเตือนอีเมล/LINE/SMS, ไม่มีส่งออกรายงาน, ไม่มีอนุมัติหลายขั้น
- ไม่มีคำนวณวันลาคงเหลือ/โควตาประจำปี, ไม่มีปฏิทินทีม, ไม่มี audit log
- ไม่มีบทบาทเกิน 3 แบบ, ไม่มีหลายภาษา/โหมดกลางคืน/ระบบชำระเงิน, ไม่มีแอปมือถือ
- ไม่มีแนบเอกสาร (`attachmentUrl`, Firebase Storage) — ต้องรอ Module 3

**ด้านข้อมูล**
- ห้ามใส่ข้อมูลจริงของบุคคลใด ๆ — ใช้ชื่อ/อีเมลสมมติเท่านั้น
- ไม่เก็บข้อมูลอ่อนไหว (เบอร์โทร เลขบัตรประชาชน ฯลฯ)
- ห้ามส่งเหตุผลการลาที่มีข้อมูลส่วนบุคคลจริงไปให้ AI ภายนอก

**ช่องว่างที่พบจริงในโค้ด ณ ตอนนี้ (ไม่ใช่แค่ "ยังไม่ทำตามแผน" แต่เป็นจุดที่ควรรู้ก่อนใช้งานจริง)**
- ปุ่มอนุมัติ/ไม่อนุมัติและกล่องเขียนความเห็น **ใช้งานได้แต่ผลไม่ถูกบันทึกจริง** — ดูเหมือนใช้ได้ แต่รีเฟรชแล้วหาย
- หน้าจัดการประเภทการลาก็เช่นกัน — เพิ่ม/แก้/ลบได้บนหน้าจอ แต่รีเฟรชแล้วกลับไปเป็น 3 รายการเดิมเสมอ และไม่เชื่อมกับดรอปดาวน์ในหน้ายื่นใบลาใหม่
- ใบลาที่ยื่นใหม่เก็บใน `sessionStorage` ซึ่งผูกกับแท็บเบราว์เซอร์เดียว — เปิดคนละแท็บหรือคนละเครื่องจะไม่เห็นใบที่เพิ่งยื่น
- ปุ่มลบใบลาใช้ได้กับใบของใครก็ได้ที่ล็อกอินอยู่ ไม่จำกัดว่าต้องเป็นเจ้าของใบ (ยังไม่ตรงกับ ACL.md ข้อ "ลบใบของตัวเองเท่านั้น")
- ช่องเขียนความเห็นฮาร์ดโค้ดชื่อผู้เขียนเป็น "สมหญิง รักงาน" เสมอ ไม่ว่าใครจะเป็นคนพิมพ์จริง
- Security Rules และโค้ดฝั่งหน้าเว็บยังไม่มีจุดไหนอ่าน/บังคับใช้ `role` เลยสักจุด
- คีย์ OpenRouter ยังอยู่ฝั่งเบราว์เซอร์ (มองเห็นได้ผ่าน DevTools) — รอทางแก้ Module 3
- มีโฟลเดอร์ `tests/` + `playwright.config.js` เริ่มไว้แล้วแต่ยังไม่ commit — งานสัปดาห์ที่ 9 ยังไม่เริ่มจริง

---

*(สรุปนี้ยึดพฤติกรรมจริงของโค้ดในโปรเจกต์เป็นหลัก ไม่ใช่คอมเมนต์ในไฟล์หรือชื่อ commit — ถ้าโค้ดเปลี่ยนหลังจากนี้ เอกสารนี้จะไม่ตามทันโดยอัตโนมัติ ต้องสั่งให้อ่านโค้ดใหม่อีกครั้ง)*
