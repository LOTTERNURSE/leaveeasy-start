# BACKLOG.md — LeaveEasy

รายการงานที่รู้อยู่แล้วว่ายังไม่ทำ/ยังไม่แก้ ณ ตอนนี้ (2026-09-24) — ไม่ใช่ของ Module 2 ทั้งหมด บางข้อตั้งใจเลื่อนไป Module 3 ตามที่ระบุไว้ใน `leaveeasy-spec.md`

---

## 1. ย้ายคีย์ AI ไปไว้ฝั่งที่ผู้ใช้แตะไม่ได้

**ปัญหา:** Module 2 ไม่ให้เขียนเซิร์ฟเวอร์ของตัวเอง ปุ่ม AI ของ US-09 (`js/ai-classify.js`, `js/ai-summarize.js`) จึงเรียก OpenRouter ตรงจากฝั่งเบราว์เซอร์ — เปิด DevTools (F12) ดู Network ตอนกดปุ่ม จะเห็นคีย์ API ที่ใช้เรียกอยู่ในคำขอ ทุกคนที่เปิดเว็บนี้เห็นคีย์ได้เหมือนกัน (ต่างจาก Firebase `apiKey` ที่ตั้งใจให้เปิดเผยได้)

**ทางแก้ (Module 3):** เพิ่มเซิร์ฟเวอร์เล็ก ๆ เป็นตัวกลาง (เช่น Cloud Functions) ให้เว็บเรียกเซิร์ฟเวอร์ตัวเองเท่านั้น แล้วเซิร์ฟเวอร์ค่อยถือคีย์และเรียก OpenRouter ต่อ

---

## 2. ฟีเจอร์ที่ยังไม่ได้ทำ (เลื่อนไป Module 3 / Sprint 2 ตามสเปก)

- **US-10 ค้นหา/กรอง/เรียงลำดับ** ในหน้ารายการใบลา — งานต่อยอด (Sprint 2)
- **US-11 แดชบอร์ดสรุป** (`dashboard.html`) — ยังไม่มีไฟล์เลย ต้องรอ Module 3
- **US-12 แนบเอกสารประกอบใบลา** (`attachmentUrl`, Firebase Storage) — ต้องใช้ Firebase Storage ซึ่งไม่อยู่ใน Module 2
- แจ้งเตือนอีเมล/LINE/SMS, ส่งออกรายงาน Excel/PDF, อนุมัติหลายขั้น, คำนวณวันลาคงเหลือ/โควตา, ปฏิทินทีม, ประวัติการแก้ไข (audit log), หลายภาษา/โหมดกลางคืน — ทั้งหมดอยู่นอกขอบเขต Module 2 ตาม `leaveeasy-spec.md` หัวข้อ 9

---

## 3. สิ่งที่เทสต์จับได้แต่ยังไม่ได้แก้

### 3.1 `js/register.js` — race condition ทำให้สมัครสมาชิกไม่สมบูรณ์ (พบจากทดสอบมือ)
`onAuthStateChanged` listener เด้งไป `index.html` ทันทีที่สมัครสำเร็จ (auth-wise) ซึ่งอาจเกิด**ก่อน**ที่ `updateProfile()` และการเขียน `users/{uid}` ใน submit handler จะเสร็จ — ยืนยันซ้ำได้จริง: บัญชีทดสอบสมัครเสร็จ redirect ไปแล้ว แต่ `displayName` เป็น `null` และเอกสาร `users/{uid}` ไม่ถูกสร้างเลย
**ผลกระทบ:** ชื่อที่ควรเป็นภาษาไทยกลายเป็นอีเมลแทน (ขัด US-01), ไม่มี role จนกว่าจะมีใครสร้างเอกสารให้ทีหลัง (ทุกจุดเช็ค role มี fallback ปลอดภัยอยู่แล้วจึงไม่ใช่ช่องโหว่สิทธิ์ แต่ใช้งานไม่ถูกต้อง)
**สถานะ:** ยังไม่แก้

### 3.2 `access-isolation.spec.js` #12/#24 — บัญชี B เปิด URL ตรงของใบลาบัญชี A แล้วเห็นข้อมูลจริง
**นี่ไม่ใช่บั๊กที่ต้องแก้โค้ดเพิ่ม** — `firestore.rules` ที่แยกสิทธิ์ตาม `requesterId` เขียนไว้เสร็จแล้วในไฟล์ local (มีคอมเมนต์ "⚠️ ยังไม่ deploy" กำกับไว้ในไฟล์เอง) แต่ **ยังไม่ได้ publish ขึ้น Firebase Console** — กฎที่ใช้งานจริงตอนนี้ยังเป็นกฎเก่าของสัปดาห์ที่ 7 (เช็คแค่ "ล็อกอินหรือยัง")
**ต้องทำ:** เจ้าของโปรเจกต์ (ผู้ใช้) ต้องเอาเนื้อหาทั้งไฟล์ `firestore.rules` ไปวางแทนกฎเดิมใน Firebase Console → Firestore Database → Rules → Publish ด้วยตัวเอง (ไม่มีใครทำแทนได้ เพราะเป็นการเปลี่ยน security setting ของระบบจริง)
**สถานะ:** โค้ดพร้อมแล้ว รอ publish

### 3.3 `leave-flow.spec.js` #22 — ดรอปดาวน์ประเภทการลาไม่คงที่ (flaky) หลังเพิ่มประเภทใหม่
เกิดบน webkit ประมาณ 1 ใน 5 ครั้งที่สังเกต: เพิ่มประเภทการลาสำเร็จจริง (ยืนยันจากตาราง `leave-types.html`) แต่เปิดหน้า `new-leave-request.html` ใหม่ทันทีแล้วดรอปดาวน์ยังว่าง (query คืนค่าเปล่าทั้งที่ข้อมูลมีอยู่จริง) รันซ้ำแล้วกลับมาปกติ
**ยังฟันธงไม่ได้** ว่าเป็นบั๊ก read-after-write ใน `js/new-leave-request.js` (ไม่มี retry เมื่ออ่านได้ผลว่างเปล่า) หรือความหน่วงของเครือข่าย/แบ็กเอนด์ในสภาพแวดล้อมทดสอบ
**ข้อเสนอ:** ถ้าต้องการความทนทานขึ้น เพิ่ม retry 1 ครั้งใน `js/new-leave-request.js` เมื่ออ่าน `leaveTypes` ได้ผลลัพธ์ว่างเปล่าผิดปกติ
**สถานะ:** ยังไม่แก้ (ยังไม่ยืนยัน 100% ว่าเป็นบั๊กจริง)

---

*(ดูรายละเอียดผลทดสอบเต็มที่ [test-results.md](test-results.md))*
