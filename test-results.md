# ผลการทดสอบ Playwright — LeaveEasy (สัปดาห์ที่ 9)

**รันเมื่อ:** 2026-09-24 (รอบล่าสุด หลัง deploy `firestore.rules` รายห้อง)
**คำสั่ง:** `npx playwright test --workers=1` · เบราว์เซอร์ `chromium` + `webkit` · ยิงเว็บจริง `https://lotternurse.web.app` กับ Firestore จริง

## สรุป

| | จำนวน |
|---|---|
| ✅ ผ่าน | **22** |
| ❌ ไม่ผ่าน | **0** |
| ⏭️ ข้าม | **8** (ต้องใช้บัญชีทดสอบ manager/hr ใน `.env` — ดูหัวข้อ "เทสต์ที่ข้าม") |
| รวม | 30 (15 เทสต์ × 2 เบราว์เซอร์) |

**เทสต์ความปลอดภัยผ่านครบ 5 ตัว × 2 เบราว์เซอร์** — สมัครบัญชีที่สองแล้วอ่าน/แก้/เขียนของบัญชีแรกไม่ได้ ทั้งผ่านหน้าเว็บและยิงคำสั่งเข้า Firestore ตรง ๆ

## ตารางผล (ผลเหมือนกันทั้ง chromium และ webkit)

| # | ไฟล์ | เทสต์ | ทดสอบอะไร | ผล |
|---|---|---|---|---|
| 1 | smoke | เปิดหน้าแรกโดยไม่ล็อกอิน | ต้องเด้งไป `login.html` | ✅ |
| 2 | smoke | เปิด leave-requests.html โดยไม่ล็อกอิน | ต้องเด้งไป login | ✅ |
| 3 | smoke | เปิด new-leave-request.html โดยไม่ล็อกอิน | ต้องเด้งไป login | ✅ |
| 4 | smoke | เปิด leave-request-detail.html?id=lr001 โดยไม่ล็อกอิน | ต้องเด้งไป login | ✅ |
| 5 | smoke | เปิด leave-types.html โดยไม่ล็อกอิน | ต้องเด้งไป login | ✅ |
| 6 | leave-flow | กรอกไม่ครบต้องไม่บันทึก | เว้นว่างทีละช่อง · วันที่สิ้นสุดก่อนวันเริ่ม · ความเห็นว่าง → เตือนและไม่บันทึก | ✅ |
| 7 | 🔒 access-isolation | (5ก) หน้ารายการของ B ไม่เห็นใบของ A | บัญชี B (คนละ context) ไม่เห็นหัวข้อใบของ A | ✅ |
| 8 | 🔒 access-isolation | (5ข) B เปิด URL ตรงของใบ A ไม่ได้ | `leave-request-detail.html?id=<idของA>` ต้องไม่แสดงข้อมูล | ✅ |
| 9 | 🔒 access-isolation | (5ค) B ยกระดับตัวเองเป็น manager ไม่ได้ | `users/{uid}.update({role:'manager'})` → `permission-denied` | ✅ |
| 10 | 🔒 access-isolation | (5ง) A อนุมัติใบของตัวเองไม่ได้ | `leaveRequests/{id}.update({status:'อนุมัติ'})` → `permission-denied` | ✅ |
| 11 | 🔒 access-isolation | (5จ) B เขียนความเห็นลงใบของ A ไม่ได้ | `approvals.add(...)` ในใบของ A → `permission-denied` | ✅ |
| 12 | leave-flow | เส้นทางหลัก: ยื่นใบลา → อนุมัติ | employee ยื่น → ไม่เห็นปุ่มอนุมัติ → manager อนุมัติ → employee เห็นสถานะใหม่ | ⏭️ ข้าม |
| 13 | leave-flow | ไม่อนุมัติต้องมีความเห็นก่อน | manager กดไม่อนุมัติก่อนมีความเห็น → เตือน → เขียนความเห็น → สำเร็จ | ⏭️ ข้าม |
| 14 | leave-flow | ลบใบลา | ใบรอพิจารณาลบได้ · ใบที่ manager อนุมัติแล้วปุ่มลบ disabled | ⏭️ ข้าม |
| 15 | leave-flow | ประเภทการลา (hr-only gate) | employee เข้าไม่ได้ · hr เพิ่ม/ลบประเภทได้และโผล่ในฟอร์ม | ⏭️ ข้าม |

## ความเปลี่ยนแปลงจากรอบก่อน

รอบก่อน (21:04–21:11 น.) ได้ **ผ่าน 21 · ไม่ผ่าน 3 จาก 24** โดย (5ข) ไม่ผ่าน เพราะตอนนั้นกฎรายห้องยังไม่ได้ขึ้น Firebase
หลังจาก `firebase deploy --only firestore:rules` เกิดผล 2 อย่าง:

1. **(5ข) ผ่านแล้ว** — บัญชี B เปิดใบของ A ไม่ได้อีกต่อไป
2. **เทสต์ 4 ตัวที่ "ยกระดับบัญชีตัวเองเป็น manager/hr" ล้มหมดด้วย `permission-denied`** — เป็นเพราะกฎทำงาน**ถูกต้อง** (ห้ามแก้ role ของตัวเอง) ไม่ใช่บั๊กของแอป ตัวเทสต์เดิมพึ่งช่องโหว่นี้อยู่ จึงแก้ตัวเทสต์ดังนี้
   - เปลี่ยนไปใช้**บัญชีทดสอบ manager/hr ถาวร** จาก `.env` ล็อกอินในหน้าต่างแยก
   - เพิ่มเทสต์ **(5ค)** ยืนยันว่าการยกระดับตัวเองต้องถูกปฏิเสธ พร้อม **(5ง)** และ **(5จ)**
   - เทสต์ประเภทการลาที่เคย flaky บน webkit (อ่าน `leaveTypes` ทันทีหลัง `add()` บางครั้งได้ผลเก่า) เปลี่ยนเป็น "เปิดหน้าใหม่ซ้ำจนเห็น ภายใน 45 วินาที" แทนการรอหน้าเดิม

## เทสต์ที่ข้าม — วิธีเปิดใช้

เทสต์ #12–15 ต้องใช้บัญชีที่มี role เป็น manager/hr จริง ซึ่ง**หน้าเว็บสร้างเองไม่ได้โดยตั้งใจ** (กฎห้ามแก้ role)

1. สมัคร 2 บัญชีที่ https://lotternurse.web.app/register.html
2. Firebase Console → Firestore → `users/{uid}` เปลี่ยน `role` เป็น `manager` (บัญชีแรก) และ `hr` (บัญชีที่สอง)
3. คัดลอก `.env.example` เป็น `.env` แล้วใส่อีเมลและรหัสผ่าน (`.env` ไม่ขึ้น GitHub)
4. รัน `npx playwright test --workers=1` ใหม่

## ข้อสังเกตเรื่องข้อมูลทดสอบ

- บัญชีทดสอบใช้อีเมล `playwright-test-...@example.com` และลบบัญชี Auth ทิ้งท้ายเทสต์ทุกครั้ง
- ไฟล์ `users/{uid}` ของบัญชีทดสอบ**ลบไม่ได้แล้ว** เพราะกฎ `allow delete: if false` จึงค้างอยู่ในฐานข้อมูล ส่วนใบลาที่ถูกอนุมัติหรือไม่อนุมัติแล้วก็ลบไม่ได้เช่นกัน (ลบได้เฉพาะใบ "รอพิจารณา") ทั้งสองอย่างเป็นพฤติกรรมตามกฎ ถ้าอยากเก็บกวาด ให้ลบเองใน Console
- ไม่แตะข้อมูล seed (`u001–u003`, `lr001–lr005`, `lt001–lt003`)
