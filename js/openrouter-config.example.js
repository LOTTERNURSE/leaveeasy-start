// ─────────────────────────────────────────────────────────────
// js/openrouter-config.example.js — เทมเพลตคีย์ OpenRouter
//
// ไฟล์นี้ commit ได้ปกติ (ไม่มีคีย์จริง)
// วิธีใช้: คัดลอกไฟล์นี้ไปตั้งชื่อ js/openrouter-config.js (ไฟล์นั้นถูก
// .gitignore ไว้แล้ว จะไม่ถูก push ขึ้น GitHub) แล้วใส่คีย์ของตัวเอง
// ขอคีย์ฟรีได้ที่ https://openrouter.ai/keys
// ─────────────────────────────────────────────────────────────

window.OPENROUTER_API_KEY = "ใส่คีย์ OpenRouter ของคุณตรงนี้";
