# ACL — ตารางสิทธิ์ผู้ใช้ LeaveEasy

อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2 (ผู้ใช้ 3 บทบาท) — ค่าที่เก็บจริงในช่อง Firestore `users.role` ต้องเป็นรหัสภาษาอังกฤษตามตารางนี้เสมอ (`employee` / `manager` / `hr`)

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **ผู้ขอลา** (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | เปลี่ยนสถานะใบลาไม่ได้เลย รวมถึง**อนุมัติ/ไม่อนุมัติใบลาของตัวเอง** · **เปิดดูใบลาของผู้ขอลาคนอื่นไม่ได้** · แก้ไขประเภทการลา |
| ✅ **ผู้อนุมัติ** (`manager`) | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ / ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ไขประเภทการลา |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่ผู้อนุมัติทำได้ · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้/ลบ ประเภทการลา · เห็นแดชบอร์ดสรุปทั้งระบบ | — |

> ⚠️ **สถานะการบังคับใช้จริงตอนนี้:** ระบบมีแค่ล็อกอิน + กฎขั้นต่ำ "ต้องล็อกอินก่อน" (สัปดาห์ที่ 7) ตารางนี้ยังไม่ถูกบังคับใช้จริงในโค้ดหรือ Security Rules — ผู้ใช้ที่ล็อกอินแล้วไม่ว่าบทบาทไหนยังเห็น/ทำได้เหมือนกันหมดในตอนนี้ การแยกสิทธิ์ตามบทบาทตามตารางนี้เป็นงานของสัปดาห์ที่ 8 (US-08 ส่วนหลัง)
